services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: kiberchekap
      POSTGRES_PASSWORD: kiberchekap
      POSTGRES_DB: kiberchekap
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kiberchekap"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
